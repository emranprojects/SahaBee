version: '3.3'
services:
  api:
    build: ./backend
    networks:
      sahabee_net:
    image: emranbm/sahabee-api:${API_VERSION}
    volumes:
      - db_volume:/database/
    environment:
      - DJANGO_SETTINGS_MODULE=sahabee.settings-production
      - PRODUCTION_IP
      - SECRET_KEY
  nginx:
    image: nginx:1.19.2
    networks:
      sahabee_net:
    ports:
      - 443:443
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - $FULLCHAIN_PATH:/etc/ssl-files/fullchain.pem:ro
      - $PRIVKEY_PATH:/etc/ssl-files/privkey.pem:ro
    depends_on:
      - api

volumes:
  db_volume:

networks:
  sahabee_net:
    driver: bridge