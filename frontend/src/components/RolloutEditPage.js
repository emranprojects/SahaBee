import React from "react";

export default function RolloutEditPage({rolloutId = null}) {
    return (
        <h4 className="mt-2 text-warning text-center">Sorry! Adding a custom Rollout has not finished implementing yet.</h4>
    )
}